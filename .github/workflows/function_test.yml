name: function_test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: configure
      run: mkdir build && cd build && cmake ..
    - name: make
      run: cd build && make test
    - name: Run Test
      run: cd build && ./test/test
